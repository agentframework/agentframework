// import { decorate, Target } from '../../../src/lib';
// import { MetadataAttribute } from '../attributes/MetadataAttribute';

class MongoDB {
  // @decorate(new MetadataAttribute(), Target.Method)
  // random: Date;
  //
  // @decorate(new MetadataAttribute(), Target.Constructor)
  // round(): any {}
}

describe('Decorate Generic', () => {
  describe('# should not able to', () => {
    it('detect agent', () => {
      expect(() => {
      
      }).toThrow();
    });
  });
});
